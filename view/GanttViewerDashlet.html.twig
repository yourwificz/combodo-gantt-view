{# @copyright   Copyright (C) 2010-2020 Combodo SARL #}
{# @license     http://opensource.org/licenses/AGPL-3.0 #}
{% extends './GanttViewer.html.twig' %}

{% block head %}
	{% if bEditMode %}
        <div class="main_header">
            <h1>{{ sTitle }}</h1>
        </div>
        <div id="workSpace{{ sId }}" style="padding:0px; overflow-y:auto; overflow-x:hidden;position:relative;margin:0 5px; max-height: 200px;">
        </div>
        <div id="gantEditorTemplates{{ sId }}">
        <div class="__template__" type="GANTBUTTONS"><!-- --></div>
    {% else %}
        <div id="workSpace{{ sId }}" style="padding:0px; overflow-y:auto; overflow-x:hidden;position:relative;margin:0 5px">
        </div>
        <div id="gantEditorTemplates{{ sId }}">
        <div class="__template__" type="GANTBUTTONS"><!--
			<div class="ganttButtonBar noprint main_header ibo-panel--header">
				<div class="ibo-panel--header-left"><div class="ibo-panel--title">{{ sTitle }}</div></div>
				<div class="ibo-panel--header-right">
                    <button onclick="$('#workSpace{{ sId }}').trigger('zoomMinus.gantt'); return false;" class=" ibo-button--icon" title="zoom out"><i class="fas fa-search-minus"></i></button>
                    <button onclick="$('#workSpace{{ sId }}').trigger('zoomPlus.gantt');return false;" class=" ibo-button--icon" title="zoom in"><i class="fas fa-search-plus"></i></button>
                    <span class="ganttButtonSeparator"></span>
                    <button onclick="ge{{ sId }}.gantt.showCriticalPath=!ge{{ sId }}.gantt.showCriticalPath; ge{{ sId }}.redraw();return false;" class=" ibo-button--icon requireCanSeeCriticalPath" title="CRITICAL_PATH"><i class="fas fa-sitemap"></i></button>
                    <span class="ganttButtonSeparator requireCanSeeCriticalPath"></span>
                    <button onclick="ge{{ sId }}.splitter.resize(.1);return false;" class="ibo-button--icon" ><i class="fas fa-chevron-left"></i></button>
                    <button onclick="ge{{ sId }}.splitter.resize(50);return false;" class=" ibo-button--icon" ><i class="fas fa-columns"></i></button>
                    <button onclick="ge{{ sId }}.splitter.resize(100);return false;" class=" ibo-button--icon"><i class="fas fa-chevron-right"></i></button>
				</div>
			</div>
			-->
        </div>
    {% endif %}
{% endblock %}